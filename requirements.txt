# Core (Stage 0 harness + tests)
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
scipy>=1.10          # reading NASA/CALCE/Oxford .mat files
pyyaml>=6.0
pytest>=7.4

# Modeling (Stage 1+). Pin a CUDA/CPU build appropriate to your machine.
torch>=2.1
scikit-learn>=1.3    # classical baselines (XGBoost/GPR/SVR live here or xgboost)
